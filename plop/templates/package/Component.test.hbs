import { render } from '@testing-library/svelte'
import {{pascalCase packageName}} from '..';

describe('{{pascalCase packageName}}', () => {
  test('should render component correctly', () => {
    const { getByText } = render({{pascalCase packageName}})

    expect(getByText('Default {{pascalCase packageName}}')).toBeInTheDocument()
  })

    // TODO: Test CustomEvent from eventDispatcher. Currently no known solutions using testing-library (https://github.com/testing-library/native-testing-library/issues/58).
});
